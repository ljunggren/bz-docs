# Read-Only Policy

The `.readonly/` directory is reserved for core specifications, architectural decisions (ADRs), and critical design documents.

## Rules for AI Agents
1. **ZERO TOLERANCE: Never write to `.readonly/`**: You are strictly prohibited from creating, modifying, editing, or deleting files in this directory. This rule has **NO EXCEPTIONS**. Any attempt to modify this directory is a critical violation of the project integrity.
2. **Consult for Guidance**: Always check `.readonly/` before proposing significant changes to ensure they align with the project's foundational goals.
3. **Report Discrepancies**: If you find that the current implementation contradicts a document in `.readonly/`, notify the user immediately rather than changing the spec.
